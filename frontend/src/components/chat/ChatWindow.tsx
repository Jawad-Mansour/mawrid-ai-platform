// Feature: AI Chatbot (Admin + Consumer)
// Layer:   Component / Chat
// Purpose: Chat interface component. Handles SSE streaming responses,
//          message history display, HITL action proposal cards (actions
//          proposed by agents shown with "Needs Approval" badge and link
//          to HITL queue). Arabic RTL support.
// API:     POST /api/v1/chat/message (SSE streaming)

export function ChatWindow() {
  return <div>ChatWindow</div>;
}
