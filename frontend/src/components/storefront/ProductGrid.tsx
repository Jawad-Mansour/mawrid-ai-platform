// Feature: Customer-Facing Storefront
// Layer:   Component / Storefront
// Purpose: Published product grid for storefront. Shows product image,
//          name, price, availability (in stock / out of stock — no exact qty).
//          Does NOT show supplier, cost price, or margin data.
// API:     GET /api/v1/store/products

export function ProductGrid() {
  return <div>ProductGrid</div>;
}
